{
  "name": "i-am-ok-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests. Emulator only, never the live project.",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0"
  }
}
